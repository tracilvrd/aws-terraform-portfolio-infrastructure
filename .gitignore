# Terraform core files
.terraform/

# State files (NEVER commit these)
terraform.tfstate
terraform.tfstate.backup

# Crash logs
crash.log
crash.*.log

# Variable files with secrets (safety)
*.tfvars
*.tfvars.json

# Override files
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# CLI config
.terraformrc
terraform.rc